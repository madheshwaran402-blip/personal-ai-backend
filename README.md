# Madheshwaran Personal AI — Backend

Python Flask server connecting React frontend to Ollama AI.

## Run locally
source venv/bin/activate
python app.py

## Endpoints
POST /chat/stream — streaming AI response
GET /health — check server status